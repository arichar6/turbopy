name: turbopy
channels:
        - defaults
dependencies:
        - python=3.7
        - numpy
        - scipy
        - pytest-runner
        - pytest
        - pytest-cov
        - pip
        - pip:
                - qtoml
                - livereload
                - formic2
                - sphobjinv
